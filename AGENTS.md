# 《天机变》项目开发核心约束 (Core Development Constraints)

**文档版本: 1.0**
**生效日期: 2025-09-27**

## 1. 总则 (General Principle)

本文件旨在为所有参与《天机变》项目的开发者（包括人类与AI代理）提供一套不可动摇的核心设计约束。在进行任何原型开发、功能实现或AI探索之前，必须将以下文档视为项目的“宪法”和“单一事实来源”。

**任何偏离以下文档所定义规则、逻辑或意图的行为，都被视为是错误的，** 除非经过正式的设计审查会议，并对相应的约束文档本身进行了版本更新。

---

## 2. 核心约束文件 (Core Constraint Documents)

### 2.1. 逻辑引擎蓝图: `card_logic_schema.md` (版本 3.0)

- **约束**: 这是游戏逻辑引擎的**唯一**蓝图。所有卡牌效果的实现，都**必须**能够被此 `v3.0` 版本的 schema 所描述和驱动。
- **禁止**: 禁止绕过此 schema 在游戏引擎中对卡牌效果进行硬编码。任何新机制的设想，都必须首先验证是否能被此 schema 兼容或扩展。

### 2.2. 游戏规则法典: `game_rules.md` (版本 2.0)

- **约束**: 这是游戏的**最终**规则集。所有游戏行为的裁定，都**必须**严格遵循此 `v2.0` 版本中的定义。
- **特别强调**:
    - **第 7.1 节 “金科玉律”**: 效果结算的六级优先级是处理所有冲突的最高准则，不可违背。
    - **第 7.2 节 “核心术语定义”**: “伤害”/“损失”、“代价”/“惩罚”等术语的区分是强制性的。

### 2.3. 卡牌设计总纲: `hexagram_interpretations.md` (版本 2.0)

- **约束**: 这是全部64张基础卡牌**唯一**的设计意图和效果描述文档。
- **要求**: 未来所有的原型开发和AI探索，其首要目标应是**忠实地复现**此 `v2.0` 版本中描述的、经过平衡性修订的卡牌效果。在这些效果被证明存在新的、未预见的破坏性漏洞之前，应视其为设计的最终形态。

---

## 3. 开发路径指引 (Guideline for Development)

基于以上约束，下一步的开发应聚焦于**“在约束下进行原型验证”**，而非“对约束本身进行再创造”。

1.  **构建最小可行引擎**: 优先实现 `card_logic_schema.md` 中最核心的动作。
2.  **测试高风险卡牌**: 优先实现和测试机制最独特的卡牌（如《蹇》、《革》、《屯》），以验证引擎的鲁棒性。
3.  **迭代与反馈**: 在严格遵守规则的前提下，通过大量模拟对局来发现新的“涌现”问题，并以最小的幅度修订卡牌效果来解决它们，同时更新 `hexagram_interpretations.md` 文档。

此 `AGENTS.md` 文件的存在，旨在确保项目在创新的同时，不会偏离其稳定、自洽的核心。